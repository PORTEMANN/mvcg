# Protocole gelé — L4 volet 2 : « deux nombres liés » pesé directement

**Gelé avant run — 2026-09-14.** Mots inconnus au gel. Suite du chantier L4
(`L4-GYROCORPS-CONTACTS.md`) : la contrainte 1 du pont P4 — ω₀ et β dérivés
d'un seul paramètre, la circulation effective κ — y était pesée indirectement
(L4_Fenetre_Branche, P au cheveu à 1,20 U). Le présent volet pèse le lien
**directement**, dans sa forme nucléaire déclarée : la loi de Kelvin.

## La forme déclarée du lien (gelée)

Convention « paramètres nus » du corpus (note P3 : « β ≈ 11 (×20 Kelvin) ») :
ω_K(k) = (κ/4π)·k², sans facteur logarithmique. Dette de forme écrite : la
forme avec logarithme ω_K = (κ/4π)k²ln(1/(kξ)) réduirait l'écart — sensibilité
chiffrée au gel : κ_eff passe de ×22,4 à ×12,9 (ξ=0,3, k=k₃). Le verdict des
deux contacts est invariant à l'extrême favorable (S− dans les deux cas à des
échelles ≫ 1) — la sensibilité est consignée, pas utilisée pour adoucir.

κ = 2π gelé (unités internes GP, ℏ=m=g=1) ; u(κ)=0 (constante de protocole,
pas une mesure) ; k₁ gelé par la table LITERATURE-HP2027 ; β=11,2 gelé (P1–P2).

## Contacts (2, registre meso, fibre ("1","1"), abs)

1. **L4_Kappa_Eff** — la moitié pente du lien : si la pente de la branche est
   d'origine Kelvin, la circulation implicite est κ_eff = 4π·β. pred :
   κ_eff implicite = κ déclarée du banc. theta = u(4πβ) =
  0.6283185307179586 (u(β)=0,05 propagée), decide=U k=1, ligne unique.
   Estimation pré-run honnête : mu_loc = 140,743, delta = 134,460,
   ratio **214,0 → S− attendu SANS suspense** : la pente exige une
   circulation ×22,4 — la démultiplication du corpus, restatée comme dette de
   circulation. Le lien à un paramètre unique échoue sur la moitié pente :
   la cohésion du milieu (verrouillage de phase, P4) doit fournir le facteur.
2. **L4_Kelvin_Gap** — la moitié gap du lien : la circulation pure ne produit
   aucun terme de gap (ω_K → 0 quand k → 0). pred : ω_K(k₁) = (κ/4π)k₁² =
   mesure du gap universel 0,62. theta = u = 0,02 (lecture déclarée du gap,
   ligne unique), decide=U k=1.
   Estimation pré-run : mu_loc = 0,01928, delta = 0,60072, ratio **30,0 →
   S− attendu SANS suspense** : une circulation seule ne fait pas le gap —
   exactement ce que P4 exclut (« ni une corde ni un gap nu »), désormais
   chiffré : le gap est une dette de structure, pas de circulation.

## Lecture d'ensemble (gelée avant run)

Le lien direct est pesé et réfuté dans sa forme nue, moitié par moitié, avec
les deux dettes chiffrées : ×22,4 côté pente (ou ×12,9 à l'extrême favorable),
×31 côté gap (0,62/0,0193). Cohérent avec l'arc L4 volet 1 : la branche à deux
paramètres tient (fenêtre P au cheveu), chaque réduction à un seul paramètre
échoue. La frontière « deux nombres liés » reste ouverte, et la machine en
publie maintenant le prix : il faut une physique de cohésion qui produise
simultanément un facteur ~20 de pente ET un gap sans tension propre.

## Anti-fraude (gelée avant run)

- La référence n'entre jamais dans le calcul ; κ et la forme Kelvin sont des
  constantes de protocole déclarées, pas des données ajustées.
- Aucune variante de forme n'est choisie pour améliorer le verdict : la forme
  nue (convention du corpus) est pesée ; la variante log est consignée comme
  sensibilité, les deux mots restent S−.
- Toute divergence mot attendu / mot découvert sera consignée, jamais corrigée
  en silence.
