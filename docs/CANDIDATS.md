# Structures candidates

Le noyau ne produit pas de phénoménologie. Il **entraîne** des \(S\)
déclarées : gel → \(U\) → métrique → \(d\).

```bash
PYTHONPATH=src python3 -m mvcg --offline --root registre campaign
```

Catalogue v0.1 : Dirac HL (identité), Dirac mauvais paquet, ASH gain,
conservation spectrale, gyro spectral, dissipation numérique Poisson,
ratio d'énergie type « BPS proxy » (pas un monopôle).

Un \(S^{+}\) ici valide le *couple* (structure, protocole), pas le monde.
