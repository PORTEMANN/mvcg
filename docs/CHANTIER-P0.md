# Chantier P0 — balance de labo

1. `*.D.json` entre dans `bits_sha256` (`d_sha256`).
2. `data/tables/h2plus_De_DEMO-2026.json` — constante déclarée, pas CCCBDB live.
3. `chain.py` — composition typée + 2-cellule `egal`.
4. Contacts `P20_H2plus_LCAO` (S− ansatz pauvre) et `P20_H2plus_table` (S+ tautologique).
5. `_verdict(..., sigma)` : seuil effectif `max(θ,σ)`.
6. Tests OTS skippés si la lib est absente.
7. `si + ε0=1` + dimension EM → `units_kill`.
